services:
  - type: web
    name: llm-segmentation-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn llm:app --host 0.0.0.0 --port 10000"
    plan: free